from <krabba.krs> import krabba;
from <stdlib.krs> import std.print as print;

bool pinched = false;
function main() -> i32 {
    if (!piched) {
        krabba.pinch();
    }
    else {
        print("Krabba!");
    }

    return 0;
}